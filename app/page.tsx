import{headers}from"next/headers";import{redirect}from"next/navigation";import{verticals}from"./lib/verticals";const groups=["生活","美容","教育","仕事","住居"];
export const dynamic="force-dynamic";
export default async function Home(){const host=(await headers()).get("host")?.split(":")[0].replace(/^www\./,"")||"";const matched=verticals.find(v=>v.domain===host);if(matched)redirect(`/compare/${matched.slug}`);return <main><header><a href="#" className="logo">まち<span>セレクト</span></a><nav><a href="#services">サービス一覧</a><a href="/disclosure">広告方針</a></nav></header><section className="hero"><p>25 LOCAL AFFILIATE SYSTEMS</p><h1>暮らす街から、<br/><em>選択をよくする。</em></h1><span>美容・生活・教育・仕事・住居。25ジャンルを市町村単位で比較し、料金だけでは見えない条件と口コミを整理します。</span><a href="#services">25ジャンルを見る →</a><small>各ジャンルは専用ドメインと共通運営基盤で管理します。</small></section><section className="results" id="services"><div className="head"><div><p>SERVICE PORTFOLIO</p><h2>ジャンル別サイト一覧</h2></div><span>全25ジャンル</span></div>{groups.map(group=><div className="portfolio" key={group}><h3>{group}</h3><div className="site-grid">{verticals.filter(v=>v.category===group).map(v=><a href={`https://${v.domain}`} key={v.slug} style={{"--accent":v.accent}as React.CSSProperties}><small>市町村 ×</small><b>{v.name}</b><span>{v.points.join("・")}</span><i>{v.domain} →</i></a>)}</div></div>)}</section><section className="compare"><div><b>01</b><h3>市町村SEO</h3><p>地域・目的・条件を組み合わせた検索意図別ページを展開。</p></div><div><b>02</b><h3>差別化データ</h3><p>総額、保証、空き、口コミなどジャンル固有情報を蓄積。</p></div><div><b>03</b><h3>通知と収益計測</h3><p>再訪通知からASP成果・報酬まで一元管理。</p></div></section><footer><a className="logo" href="#">まち<span>セレクト</span></a><p>地域サービス比較プラットフォーム</p><small><a href="/privacy">プライバシー</a> · <a href="/disclosure">広告掲載方針</a></small></footer></main>}
