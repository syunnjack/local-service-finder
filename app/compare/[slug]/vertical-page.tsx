/* eslint-disable @next/next/no-html-link-for-pages */
"use client";import{FormEvent,useMemo,useState}from"react";import type{Vertical}from"../../lib/verticals";
const cities=["新宿区","渋谷区","中野区","杉並区"];const base=[{id:"a",suffix:"セレクト",rating:4.8,reviews:186,price:9800,tag:"条件一致度 No.1"},{id:"b",suffix:"パートナー",rating:4.6,reviews:92,price:12800,tag:"口コミ注目"},{id:"c",suffix:"ローカル",rating:4.5,reviews:64,price:15400,tag:"保証充実"}];
export default function VerticalPage({vertical:v}:{vertical:Vertical}){const[city,setCity]=useState("新宿区"),[service,setService]=useState(v.services[0]),[email,setEmail]=useState(""),[consent,setConsent]=useState(false),[status,setStatus]=useState(""),[selected,setSelected]=useState(base[0]);const providers=useMemo(()=>base.map(x=>({...x,name:`${city}${v.name}${x.suffix}`})),[city,v.name]);
async function submit(e:FormEvent){e.preventDefault();const r=await fetch("/api/leads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({vertical:v.slug,providerId:selected.id,city,service,home:"未指定",email,consent})});const b=await r.json()as{error?:string};setStatus(r.ok?`${v.cta}を受け付けました`:b.error||"登録できませんでした");if(r.ok)setEmail("")}
return <main style={{"--green":v.accent} as React.CSSProperties}><header><a href="/" className="logo">まち<span>セレクト</span></a><nav><a href="#results">比較</a><a href="#guide">選び方</a><a href="#request">{v.cta}</a></nav></header><section className="hero"><p>{v.category.toUpperCase()} · LOCAL COMPARISON</p><h1>{city}の<br/><em>{v.name}を比較。</em></h1><span>料金だけでなく、{v.points.join("・")}まで透明に比べます。地域と目的を選ぶだけで候補を整理できます。</span><a href="#results">無料で比較する →</a><small>掲載内容は初期検証用の架空データです。</small></section><section className="search"><label>市町村<select value={city} onChange={e=>setCity(e.target.value)}>{cities.map(x=><option key={x}>{x}</option>)}</select></label><label>目的<select value={service} onChange={e=>setService(e.target.value)}>{v.services.map(x=><option key={x}>{x}</option>)}</select></label><label>優先条件<select>{v.points.map(x=><option key={x}>{x}</option>)}</select></label><a href="#results">絞り込む →</a></section><section className="results" id="results"><div className="head"><div><p>LOCAL RESULTS</p><h2>{city} × {v.name}</h2></div><span>{providers.length}{v.unit}・{service}の参考比較</span></div><div className="cards">{providers.map((p,i)=><article key={p.id}>{i===0&&<i>{p.tag}</i>}<div><b>{p.name}</b><strong>★ {p.rating}</strong></div><small>口コミ {p.reviews}件</small><h3>¥{p.price.toLocaleString()}〜</h3><p>{service}の参考価格</p><ul>{v.points.map(x=><li key={x}>✓ {x}</li>)}</ul><dl>{v.points.map((x,j)=><div key={x}><dt>{x}</dt><dd>{j===0?"確認済み":"掲載あり"}</dd></div>)}</dl><button onClick={()=>{setSelected(p);document.getElementById("request")?.scrollIntoView({behavior:"smooth"})}}>{v.cta} →</button></article>)}</div></section><section className="compare" id="guide">{v.points.map((x,i)=><div key={x}><b>0{i+1}</b><h3>{x}を比較</h3><p>市町村と利用目的が同じ候補をそろえ、違いが分かる形で表示します。</p></div>)}</section><section className="request" id="request"><div><p>FREE MATCHING</p><h2>{v.cta}。</h2><span>選択中：<b>{selected.name}</b><br/>提携先への送客には広告報酬が発生する場合があります。</span></div><form onSubmit={submit}><label>通知先メール<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><p>{city}・{service}</p><label className="consent"><input required type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)}/> 連絡とプライバシーポリシーに同意</label><button disabled={!consent}>{v.cta} →</button><output>{status}</output></form></section><footer><a className="logo" href="/">まち<span>セレクト</span></a><p>{city}の{v.name}を、条件まで透明に。</p><small>ドメイン候補：{v.domain}</small></footer></main>}
