/* eslint-disable react-hooks/set-state-in-effect, @next/next/no-html-link-for-pages */
"use client";import{useEffect,useMemo,useState}from"react";import{verticals}from"../lib/verticals";
type Metric={views:number;compares:number;ctas:number;leads:number;reviews:number;pendingReviews:number};const zero:Metric={views:0,compares:0,ctas:0,leads:0,reviews:0,pendingReviews:0};
export default function Dashboard(){const[days,setDays]=useState(30),[data,setData]=useState<Record<string,Metric>>({}),[loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);fetch(`/api/analytics?days=${days}`).then(r=>r.json()).then(x=>setData(x.verticals||{})).finally(()=>setLoading(false))},[days]);const rows=useMemo(()=>verticals.map(v=>({v,m:data[v.slug]||zero})).sort((a,b)=>b.m.views-a.m.views),[data]);const total=rows.reduce((a,x)=>({views:a.views+x.m.views,compares:a.compares+x.m.compares,ctas:a.ctas+x.m.ctas,leads:a.leads+x.m.leads,reviews:a.reviews+x.m.reviews,pendingReviews:a.pendingReviews+x.m.pendingReviews}),{...zero});const pct=(a:number,b:number)=>b?((a/b)*100).toFixed(1):"0.0";const max=Math.max(1,...rows.map(x=>x.m.views));
return <main className="dashboard"><header><a href="/" className="logo">まち<span>セレクト</span></a><nav><a href="/">サイト一覧</a></nav></header><section className="dash-head"><div><p>PORTFOLIO ANALYTICS</p><h1>25サイト KPI</h1><span>閲覧から比較・CTA・申込まで、収益導線の詰まりを確認します。</span></div><div className="period">{[[1,"昨日まで"],[7,"7日間"],[30,"30日間"]].map(([n,label])=><button className={days===n?"active":""} key={n} onClick={()=>setDays(Number(n))}>{label}</button>)}</div></section><section className="kpis"><article><small>アクセス数</small><b>{total.views.toLocaleString()}</b><span>ページ閲覧</span></article><article><small>比較CTR</small><b>{pct(total.compares,total.views)}%</b><span>{total.compares} 比較</span></article><article><small>送客CTR</small><b>{pct(total.ctas,total.views)}%</b><span>{total.ctas} CTA</span></article><article><small>CVR</small><b>{pct(total.leads,total.ctas)}%</b><span>{total.leads} 申込</span></article><article><small>口コミ</small><b>{total.reviews}</b><span>{total.pendingReviews}件 審査待ち</span></article></section><section className="rankings"><div className="head"><div><p>VERTICAL PERFORMANCE</p><h2>ジャンル別の成果</h2></div><span>{loading?"集計中…":`${days}日間`}</span></div><div className="metric-table"><div className="metric-row labels"><span>ジャンル</span><span>アクセス</span><span>比較CTR</span><span>送客CTR</span><span>申込</span><span>改善判断</span></div>{rows.map(({v,m},i)=><a className="metric-row" href={`/compare/${v.slug}`} key={v.slug}><span><i>{i+1}</i><b>{v.name}</b><em style={{width:`${m.views/max*100}%`,background:v.accent}}/></span><span>{m.views}</span><span>{pct(m.compares,m.views)}%</span><span>{pct(m.ctas,m.views)}%</span><span>{m.leads}</span><span className={m.views>=20&&Number(pct(m.ctas,m.views))<3?"alert":"good"}>{m.views<20?"集客を増やす":Number(pct(m.ctas,m.views))<3?"CTA改善":"継続"}</span></a>)}</div></section><section className="insight"><h2>改善ルール</h2><div><p><b>アクセス不足</b>市町村ページ・比較記事・内部リンクを増やす。</p><p><b>比較CTRが低い</b>ファーストビューと絞り込み項目を見直す。</p><p><b>送客CTRが低い</b>料金根拠、口コミ、保証、CTA文言を改善する。</p><p><b>CVRが低い</b>提携案件との一致度や入力フォームを確認する。</p></div></section></main>}
