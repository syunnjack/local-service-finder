# 地域実データの自動確認

`npm run data:refresh` は、許可済みの公式サイトだけを確認し、編集用候補を `docs/region-fact-candidates.md` に出力します。

この処理は自動公開をしません。候補を確認し、出典・確認日・地域への適用範囲を確認してから `app/lib/region-facts.ts` に反映してください。既存の掲載情報やDNS設定には触れません。

取得の成否だけを確認する場合は、`npm run data:check` を使います。ネットワークエラーや許可外のドメインがある場合は失敗として終了します。
