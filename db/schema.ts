import{index,integer,sqliteTable,text}from"drizzle-orm/sqlite-core";export const leads=sqliteTable("service_leads",{id:integer("id").primaryKey({autoIncrement:true}),providerId:text("provider_id").notNull(),city:text("city").notNull(),service:text("service").notNull(),home:text("home").notNull(),email:text("email").notNull(),consent:integer("consent",{mode:"boolean"}).notNull(),status:text("status").notNull().default("new"),createdAt:integer("created_at",{mode:"timestamp"}).notNull().$defaultFn(()=>new Date())},t=>[index("lead_city_service_idx").on(t.city,t.service,t.status)]);
